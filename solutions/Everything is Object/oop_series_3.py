#!/home/valentyna-sinichenko/miniconda3/envs/checkio/bin/checkio --domain=py run oop-series-3

# 3.1. Add an__init__()function to theCarclass that takes thebrandandmodelarguments and assigns them to the appropriate object attributes.
# 
# 3.2. You already have an objectmy_carthat was created without passing additional arguments. So, in order not to cause errors when creating an object in this way, let's add a default value for the arguments of the__init__()function They mast have values of empty string"".
# 
# 3.3. Create two newCarobjects by passing the following string values as arguments.some_car1:brand-Ford,model-Mustang;some_car2:model-Camaro. Notice, that the second car has only model, it's brand must remains default.
# 
# 
# END_DESC

# show me some OOP magic here